{
  "scripts": {
    "start": "live-server",
    "saas": "sass --watch stylesheets/application.scss:style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "sass": "^1.23.7"
  }
}
