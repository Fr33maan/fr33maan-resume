{
  "scripts": {
    "start": "live-server",
    "saas": "sass --watch src/zellus/stylesheets/application.scss:src/zellus/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "sass": "^1.23.7"
  }
}
